{
    "name": "task-master",
    "version": "1.0.0",
    "scripts": {
        "frontend": "cd frontend && npm start",
        "server": "cd server && npm start",
        "discordBot": "cd discordBot && npm start",
        "start": "concurrently \"npm run server\" \"npm run frontend\" \"npm run discordBot\"",
        "test": "cd server && npm test",
        "install_all": "cd server && npm install && cd ../frontend && npm install && cd ../discordBot && npm install"
    },
    "dependencies": {
        "dotenv": "^16.3.1",
        "react-color": "^2.19.3"
    },
    "devDependencies": {
        "concurrently": "^6.2.0",
        "nodemon": "^3.0.1"
    }
}
